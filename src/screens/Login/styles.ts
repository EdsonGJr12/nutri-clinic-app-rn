import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: "center",
        padding: 20,
        paddingTop: 100
    },
    input: {
        width: "100%",
        marginBottom: 15
    },
    inputContent: {
        width: "100%",
        marginBottom: 50
    }
});